# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this project is

`anuga_drainage` couples the 2D hydrodynamic shallow-water model **ANUGA** (overland/surface flow) with a 1D stormwater/sewer network solver (**SWMM** via `pyswmm`, or **pipedream** via `pipedream_solver`). The 2D model handles the surface; the 1D model handles the underground pipe network; the two exchange water at inlets/manholes each timestep.

The installable package is intentionally tiny — almost all the real logic lives in the per-example `run_*.py` scripts. The package only provides the shared physics of the 2D↔1D exchange.

## Install & environment

Assume work happens inside an existing conda environment that already has a current **ANUGA** built and importable. On top of that env:

```bash
pip install -e .                  # installs the anuga_drainage package
pip install pyswmm                 # SWMM backend (standard PyPI release, 2.1.0)
pip install "pipedream-solver @ git+https://github.com/anuga-community/pipedream.git@anuga"   # pipedream backend — the community fork, NOT the PyPI release
```

**Keep the package installed *editable* (`pip install -e .`).** On the maintainer's hand-built dev box (not created from `environment.yml`), the install has silently reverted to a plain copied install in `site-packages` that predates newer source — symptom: an example raises `TypeError: ... got an unexpected keyword argument` for a kwarg that *is* present in `src/` (e.g. `couple_from_inp(..., inlet_polygons=...)`). An example that happens to use only older kwargs (e.g. `real_example/run_from_inp.py`) keeps working, masking the cause. When an example "stops working", first check `pip show anuga_drainage` is editable and that pipedream is the community fork's `anuga` branch (see next paragraph) *before* debugging the script; the fix is `pip install -e . --no-deps` and `pip install -e .[pipedream]`.

**pipedream comes from the anuga-community fork, branch `anuga`** (`pyproject.toml`'s `pipedream` extra and `environment.yml` both point there). Upstream `mdbartos/pipedream` has had no commit since May 2024 and neither its PyPI release (0.2.2) nor its master works with a current numpy/pandas: 0.2.2 uses `np.bool8` (removed in numpy 2.x) and master's `SuperLink(...)` construction crashes under pandas 3's Copy-on-Write, because it hands read-only `.values` arrays to numba kernels compiled with eager, writable-only signatures. The fork's `anuga` branch is upstream master plus the two fixes (`np.bool_`; writable copies of the DataFrame-derived arrays). Both are also open upstream (mdbartos/pipedream#73, #74); if they are ever merged and released, point the extra back at upstream. There is no longer a `pandas<3` pin.

Do **not** `pip install anuga` into the conda env — ANUGA is provided by the env, not installed from `requirements.txt`. For a from-scratch setup, `environment.yml` builds a conda env with ANUGA 3.3.6 from conda-forge (Python 3.12) plus this package and both backends (`conda env create -f environment.yml`). The maintainer's dev box instead runs ANUGA from source (3.3.6.dev) on Python 3.14, which conda-forge ANUGA does not yet build for.

The package's own logic is tested under `tests/` (pytest). Note the `test_*.py` files under `examples/` are unrelated — they are standalone runnable scripts, not part of the pytest suite.

### CI and platform support
Three GitHub Actions workflows (`.github/workflows/`):
- `tests.yml` — fast lane, no ANUGA: installs `-e .[test]` on Python 3.10/3.12 and runs the pure-logic tests (geometry, `.inp` parser, `calculate_Q` with explicit `g`). ANUGA-dependent tests skip.
- `conda-env.yml` — validates `environment.yml` end to end (conda-forge ANUGA + both backends via micromamba) and runs the **full** suite on **ubuntu-latest + windows-latest**.
- `pip.yml` — installs ANUGA + this package from **PyPI wheels** and runs the full suite on **ubuntu-latest, windows-latest and macos-latest** (Apple Silicon). This is the lane that exercises the published wheels, so it catches wheel-packaging issues — it found the macOS arm64 missing-libomp bug, fixed in ANUGA 3.3.7 (pinned here), and the meshpy Windows wheel that didn't bundle its MinGW runtime DLLs (`libstdc++-6.dll`, `libgcc_s_seh-1.dll`), so `import meshpy._internals` failed on a clean pip install. That meshpy bug (`inducer/meshpy#150`) is **fixed in meshpy 2026.1** — cibuildwheel 4.0's auto-`delvewheel` bundles the DLLs under `meshpy.libs/` (verified on the PyPI wheel; our manual PR #151 was closed as redundant once cibuildwheel 4 landed via #152). The Windows `continue-on-error` workaround has been **removed** — all three OSes are now required.

macOS distribution is split and asymmetric, which is why it needs its own lane:
- **conda-forge** ANUGA built for `osx-64` (Intel) **only** — no `osx-arm64`. So `macos-latest` (arm64) couldn't use the conda `environment.yml`; Intel `macos-13` runners exist but are scarce, so conda-env omits macOS. (osx-arm64 is being added to the feedstock **natively** — `provider: {osx_arm64: default}` on Azure's native arm64 agents, conda-forge/anuga-feedstock#17; the cross-compile route was abandoned because this recipe's meson build can't resolve numpy via pkg-config when cross-compiling.)
- **PyPI** ANUGA wheels are `macosx_*_arm64` (Apple Silicon) **only** — no Intel macOS wheel. So Apple Silicon is covered by `pip install anuga`. (Through 3.3.6 these wheels were not self-contained — the macOS arm64 wheel needed `libomp` and the Windows wheel had unbundled DLLs; ANUGA 3.3.7 fixed this by running `repairwheel` on all platforms, so plain `pip install anuga` now works.) Note `macos-latest` being arm64 means the `pip` lane already tests ANUGA on Apple Silicon (via the arm64 wheel).

### Pending CI follow-ups
- **Done:** `environment.yml` pins conda-forge `anuga=3.3.7` (published 2026-06-07).
- Once conda-forge ships **osx-arm64** (feedstock#17, native build), add a **`macos-latest` (arm64) lane to `conda-env.yml`** — `macos-latest` is arm64, so this tests the conda arm64 package natively. (Less critical than it was: the native feedstock build already runs the test suite on arm64.)
- **Done:** dropped the Windows `continue-on-error` in `pip.yml` — meshpy **2026.1** (2026-06-09) ships a self-contained Windows wheel (cibuildwheel 4.0 auto-delvewheel; `inducer/meshpy#150` fixed, PR #151 closed redundant), so the pip lane now requires Windows too.

### Project decision: standard pyswmm + our own `calculate_Q`
We target the **standard pyswmm release** (currently 2.1.0 from PyPI) and compute the 2D↔1D exchange flux ourselves in Python via `anuga_drainage.calculate_Q`. We do **not** use the patched pyswmm fork (the `2D-coupling` branch with `node.create_opening(...)` / `sim.coupling_step(...)` used by `archive/pyswmm_with_openings/`); that API does not exist in the standard release. Treat `archive/pyswmm_with_openings/` as legacy/reference, not the active path.

### pyswmm 2.1.0 stepping constraints (important)
Stock pyswmm 2.1.0 is **whole-second resolution**:
- `sim.next()` was removed — step the iterator with `next(sim)`.
- `sim.step_advance(...)` feeds `swmm_stride`, whose binding requires an **`int`** (whole seconds). Even `1.0` (a float) raises `TypeError`. Always pass `int(dt)`, and keep `dt` a whole number of seconds.
- `sim.current_time` also only resolves to whole seconds.

Consequence: **SWMM coupling exchanges at 1-second granularity.** The SWMM examples use `dt = 1.0` (yield/coupling step) and rely on `time_average` smoothing for stability. Sub-second coupling is *only* available on the **pipedream** path (`superlink.step(dt=...)` is pure Python — `run_pipedream.py` uses `dt=0.05`). Note ANUGA still sub-steps internally at its CFL timestep regardless of the yieldstep, so a 1 s yieldstep does not hurt ANUGA's own mass conservation — only the coupling-exchange frequency.

**pipedream's internal step is decoupled from the coupling step.** pipedream's semi-implicit solver is unstable if stepped once at a coarse coupling `dt` (1 s blows up — huge depths/velocities → tiny ANUGA timestep). `PipedreamBackend(max_step=)` / `couple_from_inp(pipedream_max_step=)` hold `Q_in` fixed over the coupling `dt` and advance pipedream in `ceil(dt/max_step)` internal sub-steps, so the exchange/yield stays coarse (1 s) while the 1D integration is refined. The more `internal_links` (shorter sub-conduits), the smaller `max_step` must be (CFL): `run_from_inp.py` uses `internal_links=6 @ max_step=0.01`. `max_step=None` (default) steps once at `dt`, preserving the hand-built examples. SWMM doesn't need this — it routes at its own `.inp` ROUTING_STEP.

## Running examples

Each example is run by executing a script from inside its own directory (the scripts use relative paths like `./swmm_input_short.inp`):

```bash
cd examples/simple_culvert_example
python run_swmm_short.py      # ANUGA + SWMM (pyswmm)
python run_pipedream.py       # ANUGA + pipedream
python run_boyd.py            # ANUGA-only reference using a Boyd culvert operator
```

Naming convention: `run_<backend>.py` selects the 1D backend (`swmm`, `pipedream`, `boyd`). The `boyd` variants use ANUGA's built-in culvert operator with no external 1D solver — they are the physical reference the coupled runs are validated against.

Outputs: ANUGA writes `<outname>.sww` (set via `domain.set_name(...)`); SWMM reads `*.inp` and writes `*.rpt`/`*.out`. Many scripts also save `Figure*.png` and, when `visualise`/plotting is enabled, pop up matplotlib windows (and may `input('Enter key ...')` at the end — they block waiting for a keypress).

## The package (`src/anuga_drainage/`)

- `coupling.py` — `calculate_Q(head1D, depth2D, bed2D, length_weir, area_manhole, cw, co, eps, g=None)`: the core exchange-flux physics. Implements weir/orifice equations from Leandro & Martins (2016). Sign convention: **positive Q = water leaving the 2D surface and entering the 1D network**; negative Q = surcharge back up onto the surface. Vectorised over numpy arrays of inlets. `g` (gravity) defaults to `None`, which lazily imports ANUGA's value; pass `g` explicitly to use the function (and `Coupler`, which forwards it) without ANUGA, e.g. in tests. Exported at top level: `from anuga_drainage import calculate_Q`.
- `inlet_initialization.py` — `initialize_inlets(...)` builds ANUGA `Inlet_operator`s automatically from a SWMM `.inp` file: reads junction nodes (via `pyswmm`) and constructs a regular n-sided polygon region of the given manhole area at each node's coordinates. `n_sided_inlet(...)` is the geometry helper. Node map coordinates come from `read_inp_coordinates(inp_path)` in this module — a small zero-dependency parser of the `.inp` `[COORDINATES]` section (returns a DataFrame with `X_Coord`/`Y_Coord` indexed by node id). This replaced the former `hymo` dependency, which is unmaintained and not on PyPI; do **not** reintroduce it. `initialize_inlets` takes that coordinates DataFrame as its 3rd argument. (`pyswmm`/`anuga` are imported lazily inside `initialize_inlets` so the pure helpers stay importable for testing without ANUGA.)
- `inlet_catalogue.py` — a named inlet **asset catalogue**, ported from Simple_SW_Inlets and pure (no ANUGA/SWMM). `InletSpec(name, clear_area, effective_perimeter, blockage=0)` exposes `operational_area`/`operational_perimeter` (geometry derated by blockage); `INLET_LIBRARY` is the catalogue of standard grate/lintel/combo specs; `load_inlet_library(path)` reads a catalogue from a TOML file (`[inlets.<name>]` tables — quote names containing `.`); `resolve_inlet_spec(name|InletSpec, library, blockage)` resolves and derates one. All exported at top level. The resolved `operational_area`/`operational_perimeter` feed the orifice area / weir length of `calculate_Q` via `couple_from_inp(inlet_specs=...)` (see `factory.py`).
- `coupler.py` — the per-step coupling driver, factored out of the duplicated example loops. `Coupler(inlets, beds, weir_lengths, manhole_areas, backend, time_average, clamp=...)` owns the read-depths → `calculate_Q` → `smooth_Q` → optional `limit_outflow` → backend step → feed realised flux back via `set_Q` sequence; `coupler.step(dt)` returns `CouplingStep(Q_in, anuga_flux)`. The 1D-backend differences live behind `SwmmBackend(sim)` (heads from junction nodes, `int(dt)` stride, realised flow from node statistics) and `PipedreamBackend(superlink)` (heads = `H_j`, feeds back `-Q_in`). `smooth_Q`/`limit_outflow` are exported as standalone pure functions. The backends also expose independent pipe-side volume accessors (`pipe_volume`, `coupling_inflow_volume`, `outfall_volume`) used by `VolumeBalance`, plus **backend-agnostic 1D-network state accessors** — `get_heads()`, `node_depths()` (depth above invert at the coupled nodes), `conduit_names()` and `conduit_flows()` (one value per conduit: SWMM `link.flow` / pipedream superlink `Q_uk`). These let a run script record and plot the pipe network without knowing the backend (used by `real_example/real_example.ipynb`, which switches solver from a single `backend` variable).
- `volume_balance.py` — `VolumeBalance(domain, coupling_inlets, backend, inflow_operators)`: a per-step mass-balance audit that **localises** where water is lost/gained. It measures each subsystem from its own bookkeeping (ANUGA `get_total_applied_volume`/`get_water_volume`/`get_boundary_flux_integral`; the backend's pipe-side accessors) and computes three residuals: `R_anuga` (ANUGA closes — ~machine precision), `R_pipe` (pipe closes — ~0 for simple pipedream networks; exposes SWMM's finite-difference loss, and pipedream's own ~0.1–1%-of-throughput numerical mass balance on surcharged/complex networks, dominated by its internal-junction storage), and `R_couple` (the surface↔pipe handoff is consistent — ~0 when the coupling conserves). `loss = R_anuga + R_pipe + R_couple`. Baselines are captured on the first `step()` (not construction — the pre-evolve domain volume is unphysical). `.summary()`/`.to_dataframe()`/`.plot()` for reporting. Passing the `CouplingStep` to `step()` also records a **per-inlet** breakdown — `requested` (Q_in·dt) vs `accepted` (into the sewer) vs `removed` (actual ANUGA exchange) — so `reject = requested − accepted` (sewer capacity) and `drying = accepted + removed` (the inlet over-drawn) are localised per inlet. Pass `outfall_inlet=<index>` when the outfall water is **returned to ANUGA** at that inlet (as `simple_culvert` does): it's subtracted from that inlet's `removed` (shown in its own `outfall` column, so `drying` isn't inflated) and treated as an internal transfer in `R_couple`. Leave it `None` when the outfall water **leaves the system** (as `real_example` does — water exits at the outfall): then it's a system sink counted in `loss`, not `R_couple`. Getting this wrong mis-attributes the whole outfall volume to `R_couple`. **Timing:** call the audit at the *top* of the loop (where ANUGA's applied volume and the SWMM stats are aligned), passing the *previous* step's `CouplingStep`; reading at the bottom misaligns `inlets_anuga` (applied next evolve) from `inlets_pipe` by one step. Wired into `simple_culvert_example/run_swmm_short.py` as the reference; for that run `R_anuga`/`R_couple` are ~1e-12 and the whole loss is `R_pipe` (SWMM). With `clamp=False` the audit shows `R_couple` becoming nonzero (the requested draw dries an inlet) — i.e. `limit_outflow` is what keeps the handoff conservative.
  - **pipedream `R_pipe` is the solver's own numerics, not the coupling (investigated, don't "fix" it with a correction).** Diagnostic: inject a known volume into an isolated `SuperLink`, then step with `Q_in=0` — the true volume must be constant. pipedream conserves its *internal* (linearised) continuity exactly, but the *physical* water volume drifts ~0.1% partially-full and ~0.5–1% surcharged: its semi-implicit linearised continuity + Preissmann slot don't conserve physical volume exactly during redistribution/surcharge (the slot is fictitious water pipedream genuinely carries in its state). This is the **same class as SWMM's finite-difference loss, ~10× smaller** — so we **report it, don't correct it**. A coupling-side "ascertain the discrepancy and add it back at an inlet" correction would inject *phantom* water to cancel the solver's own behaviour, breaking real conservation and being inconsistent with how SWMM's larger loss is left visible. The flow-area `pipe_volume` (`A_ik·dx_ik` + pit + superjunction) is the correct *physical* measure — exact right after injection; `PipedreamBackend.node_volume()` uses pipedream's `compute_storage_volumes()` (storage-curve aware, depth clamped at `min_depth`) because the old linear `A_sj·(H_j−z_inv_j)` goes **negative** when a superjunction's head drops below its invert. If exact closure is ever needed, the lever is a smaller pipedream `dt` (linearisation error shrinks with `dt`), not a coupling fudge.
- `hydrograph.py` — `HydrographLogger(names)`: optional per-inlet, per-step hydrograph logging for the coupling. Records surface depth, 1D head, exchange Q split into capture (+) / surcharge (−), an approach-flow estimate and bypass, plus cumulative volumes; `to_dataframe(name)` / `write_csv(dir)` emit one CSV per inlet whose columns are a **superset** of the Simple_SW_Inlets viewer schema (so `viewer.py` opens them). `record(...)` is pure (plain arrays in), so it is unit-tested without ANUGA. `Coupler(logger=...)` feeds it each `step()` (approach flow ≈ region-averaged momentum × √area); `couple_from_inp(log_hydrographs=True)` attaches one, reachable at `coupling.coupler.logger`. Exported at top level.
- `inp.py` — bridges SWMM's `.inp` to pipedream (which has **no input file format** — its networks are pandas DataFrames). `read_inp(path)` parses a `.inp`'s network sections (`[JUNCTIONS]`/`[OUTFALLS]`/`[CONDUITS]`/`[XSECTIONS]`/`[COORDINATES]`) into an `InpNetwork` of DataFrames — a zero-dependency line scanner in the spirit of `read_inp_coordinates`. `inp_to_pipedream(inp, manhole_area=, pit_area=, h_0=)` maps it to pipedream `(superjunctions, superlinks)`: junctions+outfalls → superjunctions (outfalls get `bc=True`), conduits+xsections → superlinks. Shape handling is **per-shape, not positional** (`SHAPE_MAP` for names + `_shape_geometry` for params): circular/rect/parabolic/ellipse copy height/width directly, but **triangular** converts SWMM's top width → pipedream's side slope `m = Geom2/(2·Geom1)`, **trapezoidal** averages SWMM's two bank slopes into pipedream's single `m`, and **force_main** keeps the diameter but uses a default Preissmann-slot ratio for `g2` (SWMM's `Geom2` there is roughness, not geometry). SWMM names ellipses `HORIZ_ELLIPSE`/`VERT_ELLIPSE`. `IRREGULAR` raises `NotImplementedError` (needs `[TRANSECTS]`); shapes pipedream lacks (EGG, HORSESHOE, …) raise `ValueError`. The triangular conversion is round-trip-verified against pipedream's area formula in the tests. This lets a **single `.inp` drive either backend** and guarantees both model the *same* sewer (currently the hand-coded pipedream DataFrames can differ from the SWMM `.inp` — e.g. `real_example.inp` is 7 nodes/5 conduits but `run_pipedream.py` hardcodes 5/4). `.inp` doesn't carry a few pipedream-only params, so `manhole_area`/`pit_area` storage areas default (and `internal_links` is passed to `SuperLink`). Pure mapping — unit-tested without ANUGA. Validated by rebuilding `simple_culvert`'s pipedream `SuperLink` from `swmm_input_short.inp` (3 superjunctions/2 superlinks, builds and steps).
- `factory.py` — `couple_from_inp(domain, inp_path, backend='swmm'|'pipedream', manhole_area=, n_sides=, inlet_polygons=, time_average=, clamp=, cw=, co=, internal_links=, pit_area=, pipedream_max_step=, ...)`: the one-call setup. Parses the `.inp`, auto-creates an ANUGA `Inlet_operator` (regular-polygon region of `manhole_area`) at each `[JUNCTIONS]` node, builds the 1D backend (a pyswmm `Simulation` or a pipedream `SuperLink` from `inp_to_pipedream`), and returns a `Coupling` you drive directly: `coupling.step(dt)` (runs the exchange and, if attached, the volume audit with the correct top-of-loop/previous-step timing), `coupling.add_volume_balance(inflow_operators=, outfall_inlet=)` (attaches a `VolumeBalance`, exposed as `coupling.volume_balance`), and `coupling.close()` (delegates to the backend's `close()` — closes the SWMM `Simulation`, no-op for pipedream). It also exposes `coupler`, `inlets{name→op}`, `backend`, `handle`, `inp` and `domain`. **Junctions are coupled to the surface; outfalls are boundaries** — SWMM handles its own; for pipedream they're `bc=True` superjunctions held at their invert (free drainage). To support that, `PipedreamBackend(superlink, coupled_indices=, H_bc=, outfall_indices=, max_step=)` now couples a *subset* of superjunctions, passes `H_bc` for the bc nodes, sub-steps internally at `max_step` (see the stepping-constraints section above), and **tracks the outfall outflow** (accumulating `Q_dk` at superlink ends discharging to the `outfall_indices`, so `outfall_volume()` is no longer 0 and `VolumeBalance`'s `R_pipe` closes on the bc-outfall path); all three default to the previous all-coupled/no-bc/zero-outfall behaviour, so the hand-built examples are unchanged. So a coupled model is now "write one `.inp`, pick a backend, run the evolve loop". (The upstream inflow, boundaries and any outfall-return policy still live in the run script.) `couple_from_inp` needs ANUGA (lazy import); tested via an ANUGA+pipedream integration test plus pipedream-only `PipedreamBackend` subset/`H_bc`/outfall-tracking tests. **Living examples:** `simple_culvert_example/run_from_inp.py` and `real_example/run_from_inp.py` (`python run_from_inp.py [swmm|pipedream]`) drive the whole coupling from the one `.inp` — both show `R_anuga` ~1e-13 (ANUGA conserves) and a tiny `R_couple` (the coupling conserves) with the loss isolated to `R_pipe`: SWMM's finite-difference loss (`simple_culvert` ~0.08 m³) and pipedream's own linearisation/surcharge residual (`simple_culvert` ~3 m³ at `pipedream_max_step=0.01`, a floor that doesn't shrink much further with the inner step; real_example similar). `simple_culvert`'s `run_from_inp.py` passes `inlet_polygons={'Inlet':…, 'Outlet':…}` to give those two inlets a channel-spanning footprint — the `.inp` only carries a *point* per junction, so the auto regular polygon is too narrow for the channel and flow overtops the embankment instead of being captured (matches the hand-coded `run_swmm_short.py`/`run_pipedream.py` regions). Three things matter for pipedream here (the third is the internal sub-stepping above): `simple_culvert`'s culvert is **`RECT_CLOSED`** (a box culvert surcharges cleanly via the Preissmann slot — `RECT_OPEN` lost the overflow), and `inp_to_pipedream` defaults superjunction **`max_depth=inf`** (`cap_max_depth=False`): SWMM *floods* above a node's MaxDepth (conserving), but pipedream has **no flooding model** and silently caps/loses surcharge at `max_depth`, so the cap is dropped and surcharge is instead returned to the 2D surface by the coupling. (Honouring the `.inp` MaxDepth via `cap_max_depth=True` reintroduces that loss — it was the cause of a ~151 m³ phantom `R_pipe` before the fix.) `couple_from_inp` also accepts `inlet_specs={junction: name|InletSpec}`, `library=`, and `blockage=` (scalar or per-junction dict): an assigned spec's `operational_area`/`operational_perimeter` drive that junction's `calculate_Q` flux (area_manhole / length_weir), **decoupled from the surface footprint** — the ANUGA region and pipedream storage still come from `manhole_area`/`inlet_polygons`, so a small grate opening no longer shrinks the footprint. Junctions without a spec keep the footprint-derived geometry (existing behaviour unchanged).
- `viewer.py` — `HydrographViewerApp`, a Tkinter dashboard (ported from Simple_SW_Inlets) for inspecting the per-inlet hydrograph CSVs: four diagnostic plots per inlet + a View-menu folder-combined hydrograph, HiDPI-aware (`Xft.dpi`) with live font sliders. The combined-view merge/cumulative maths is a pure, tested `combine_hydrographs(csv_paths)` helper. Lives behind the `[viewer]` extra (`matplotlib`; Tkinter is stdlib) and the `anuga-drainage-viewer` console script (`viewer:main`); it is **not** imported by `__init__`, so the package core stays GUI-free. Launch with `anuga-drainage-viewer` or `python -m anuga_drainage.viewer`.

## The coupling loop (the key architecture to understand)

Every coupled run script drives the loop through `anuga_drainage.Coupler` (above): per step they call `coupler.step(dt)` (or `coupling.step(dt)` for the `couple_from_inp` ones) and rely on `VolumeBalance` for the audit. The two formerly-experimental pipedream scripts (`culvert_example/run_pipedream_culvert.py`, `real_example/run_pipedream.py`) — the culvert one preserves its historical `cw=co=1.0`; `real_example/run_pipedream.py` additionally had real bugs fixed (a reassignment orphaned inlet4 and duplicated the outlet into both junction slots) so its 5th superjunction now correctly couples to its own outlet ANUGA region, and `calculate_Q` + smoothing replaced the old inline weir formula.

**All coupled scripts were restructured to a single consistent template (2025 cleanup): a parameter block, then numbered sections `1. ANUGA domain` / `2. coupling inlets + 1D network` / `3. VolumeBalance` / `4. evolve loop` / `5. report`.** The dead scaffolding was removed (section-header `print()` walls, unused `Q_iks`/`Q_uks`/`Q_dks` storage, the duplicated manual mass-balance computation that shadowed `VolumeBalance`, and the broken `visualise` block that did `np.vstack(H_js)` on a never-appended list). Plotting now uses the backend-agnostic accessors (`coupler.backend.get_heads()` etc.) and `VolumeBalance.plot()`, and every script gates its matplotlib output behind a `visualise` flag (default `False`). The restructure kept each script's `Coupler` parameters (so the physics is unchanged); the per-component `VolumeBalance` residuals are now the headline diagnostic each script prints. The yshape examples were rewritten from scratch: `yshape/pipedream_yshape.py` → `yshape/run_pipedream.py` (was hand-rolling the weir formula + volume balance; now uses `Coupler`/`calculate_Q`/`VolumeBalance`, with `PipedreamBackend(max_step=0.05)` because the full-perimeter weir surcharges pipedream at a 1 s step), and `yshape/test_Yshape_flooding_offset.py` → `yshape/run_swmm.py` (was the **legacy patched-pyswmm-fork** path — `create_opening`/`coupling_step`/`overland_depth`, which standard pyswmm lacks; now a `couple_from_inp` swmm example off `2inlets_short.inp`). Its `2inlets_short.inp` outlet conduit was resized from a pathological 0.05 m to 0.25 m (and C1 1.0→0.5 m) so it models the same sewer as the pipedream twin; SWMM still shows a sizeable `R_pipe` there because the small network surcharges heavily, but `R_anuga`/`R_couple` stay ~1e-11 (the coupling conserves).

The underlying structure each script follows (and what `Coupler` encapsulates) — preserve this when editing or adding examples:

1. **Build the ANUGA domain** — mesh, `topography` elevation function, friction, boundary conditions, and an upstream inflow `Inlet_operator`.
2. **Define coupling regions** — for each shared inlet/outlet, an `anuga.Region` polygon and a zero-Q `anuga.Inlet_operator`. Capture each region's bed elevation (`inlet.get_average_elevation()`) into an `anuga_beds` array, plus parallel `anuga_length_weirs` / `anuga_area_manholes` arrays. Index order of these arrays must line up with the 1D nodes.
3. **Build the 1D model** — SWMM: `Simulation('./*.inp'); sim.start()` then grab `Nodes`/`Links` by name. Pipedream: construct `superjunctions`/`superlinks` DataFrames and a `SuperLink`.
4. **`for t in domain.evolve(yieldstep=dt, outputstep=out_dt, finaltime=ft):`** — each yield:
   - read 2D state at each inlet: `get_average_depth()`, `get_average_stage()`, water volume;
   - read 1D heads (`node.head` / `superlink.H_j`);
   - `Q_in = calculate_Q(heads, anuga_depths, anuga_beds, weirs, manholes)`;
   - **temporal smoothing** (critical for stability): `Q_in = ((time_average - dt)*Q_in_old + dt*Q_in)/time_average`. Oscillation control relies on this plus a small routing step in the `.inp`;
   - clamp so you never remove more water than the 2D cell holds (`Q_limit = anuga_volumes/dt`);
   - advance the 1D model by `dt` (SWMM: `node.generated_inflow(Q); sim.step_advance(int(dt)); next(sim)` — see the pyswmm 2.1.0 constraints above. Pipedream: `superlink.step(Q_in=Q_in, dt=dt)`);
   - feed the **actual** realised 1D flow back into ANUGA via `inlet_op.set_Q(...)`. For SWMM this is derived from node statistics (`lateral_infow_vol`, `flooding_volume`); sign is flipped relative to `calculate_Q`'s convention.

5. **Volume-balance check** — scripts continuously compute `loss = total_volume_real - total_volume_correct`, where real = ANUGA water volume + sewer volume and correct = cumulative inflow + boundary flux (+ initial pipe volume). This conservation diagnostic is the primary signal of whether a coupling change is correct; watch it when modifying flux logic.

### Gotchas
- SWMM/pyswmm sign and bookkeeping differs from pipedream — `Q_in` sign passed to `set_Q` is **not** the same across backends (compare `run_swmm_short.py` line ~321 vs `run_pipedream.py` line ~235). Don't blindly copy between backends.
- `calculate_Q` returns positive for surface→pipe; some scripts negate it when setting the ANUGA operator and others use SWMM's realised flow instead. Trace the specific script.
- Mesh refinement (`rf`) must be fine enough that inlet polygons don't overlap walls/each other — comments in the scripts flag this.
- **ANUGA API drift:** some examples were written against older ANUGA and need small fixes to run on a current build. Known: `create_domain_from_regions(...)` no longer accepts `mesh_filename` (the mesh is built in-memory — drop the kwarg). Expect similar minor signature changes when reviving older scripts.

## Documentation

Sphinx docs (MyST Markdown) live in `docs/`, published on Read the Docs via `.readthedocs.yaml` (installs the package + `docs/requirements.txt`; `conf.py` mocks `anuga`/`pyswmm`/`pipedream_solver` so autodoc imports — the package itself imports without ANUGA). Pages: index, installation, quickstart, **tutorial** (a notebook), coupling, diagnostics, inp_format, api (autodoc). Build locally with `pip install -e .[docs]` then `sphinx-build -b html docs docs/_build/html`.

The tutorial is a **pre-executed Jupyter notebook** (`docs/tutorial.ipynb`) rendered by `myst-nb` with `nb_execution_mode = "off"` (RTD has no ANUGA, so it can't re-execute — it renders the committed outputs/plots).

**Editing workflow — jupytext pairing (single source of truth).** The notebook is **paired with `docs/tutorial.py`** (percent format) via jupytext (the pairing lives in the notebook metadata; `jupytext` is in the `[docs]` extra). Edit *either* file — `docs/tutorial.py` (clean, diff-reviewable; the one to read in PRs) or the `.ipynb` interactively in Jupyter — then run `jupytext --sync docs/tutorial.ipynb` to propagate the change both ways. **Commit both files.** (This replaced the old hand-written `docs/_make_tutorial.py` generator, which kept drifting from hand-edits to the notebook — don't reintroduce that pattern.)

After changing the *source*, refresh the committed **outputs** (RTD renders them) by executing in place: `OMP_NUM_THREADS=4 jupyter nbconvert --to notebook --execute --inplace docs/tutorial.ipynb` (needs ANUGA+pipedream; **don't** set `MPLBACKEND=Agg` or the inline plots/animation won't embed — the notebook uses `%matplotlib inline` and `rc('animation', html='jshtml')`). Then `jupytext --sync` once more so the `.py` matches. Caveat: `nbconvert --inplace` **overwrites `tutorial.ipynb`**, so never run a notebook-writing step against a notebook with un-synced/un-committed edits without backing it up first.

## Examples directory map

- `simple_culvert_example/` — canonical minimal channel + culvert; the best reference for the coupling pattern. Has `boyd`, `swmm` (short/long inp), and `pipedream` variants. A local copy of `coupling.py` lives here (the scripts now import from the installed package instead — see git history).
- `culvert_example/` — culvert on a meshed terrain (`terrain.csv`/`.msh`), boyd vs pipedream.
- `yshape/` — Y-shaped pipe topology (two inlets feeding one lower outlet). `run_pipedream.py` (hand-built pipedream) and `run_swmm.py` (`couple_from_inp` off `2inlets_short.inp`). The legacy fork-based tutorial `archive/pyswmm_with_openings/how_to_ANUGA_SWMM_coupling.md` describes the old `test_Yshape_flooding_offset.py` (now `run_swmm.py`) and is reference only.
- `real_example/` also ships a notebook walkthrough, `real_example.ipynb` (standalone, executed-and-committed; run from the example dir).

Archived (outside `examples/`, not the active path):
- `archive/pyswmm_with_openings/` — **legacy/reference.** Experiments against a patched `2D-coupling` pyswmm fork (`create_opening`/`coupling_step`/`overland_depth` API) that is *not* in the standard release. Moved out of `examples/` — see "Project decision" above.
- `real_example/` — a real catchment: terrain/kerb/wall CSVs, a DRAINS-exported network, and `run_{no_pipes,swmm,pipedream}.py` (hand-built) plus `run_from_inp.py` (the `couple_from_inp` living example). `simple_culvert_example/` also has a `run_from_inp.py`.
